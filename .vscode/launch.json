{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Server",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/web-server/main.go",
            "cwd": "${workspaceFolder}",
            // "args": [
            //    "serve",
            //]
        }
    ]
}